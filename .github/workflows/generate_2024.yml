name: Generate 2024 Excel and CSV files

on:
  workflow_dispatch:

jobs:
  prepare:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Trigger Copilot Task
        run: |
          echo "Preparing Copilot automation for 2024 Excel generation."
